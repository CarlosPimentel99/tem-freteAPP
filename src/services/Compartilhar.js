import { Share } from 'react-native';

const Compartilhar = (conteudo) => {    
  // try{
  // Share.open(options)
  // .then((res) => {
  //   console.log(res);
  // })
  // .catch((err) => {
  //   throw new Error(err);
  // });
  // }catch(error){
  //   Alert.alert('Não foi possível compartilhar o conteúdo. Erro:' + error.message);
  // }
};

export default Compartilhar;